const S_MIN=60,S_HR=3600,S_DAY=86400,S_WEEK=604800,units={minute:60,minutes:60,min:60,mins:60,m:60,hour:3600,hours:3600,hr:3600,hrs:3600,h:3600,day:86400,days:86400,d:86400,week:604800,weeks:604800,wk:604800,wks:604800,w:604800},pattern=/(?<now>now)|-(?<count>\d+)(?<unit>\w+)/i,k_now="now",k_count="count",k_unit="unit",seconds=ds=>{if(null==ds)return 0;const ms=x=>Math.round(new Date(x).getTime()/1e3),digsOnly=+ds==ds;if(digsOnly)return ms(+ds);const str=`${ds}`.toLowerCase(),m=str.match(pattern);if(m){if(m.groups.now)return ms(Date.now());const u=m.groups.unit;if(u in units){const delta=m.groups.count*units[u];return ms(Date.now()-1e3*delta)}}return ms(ds)};export const time={seconds:seconds};